{
  "identifier": "default",
  "description": "Default capabilities for the app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "shell:allow-execute",
    "core:event:default",
    "core:event:allow-emit",
    "core:event:allow-listen"
  ]
}
